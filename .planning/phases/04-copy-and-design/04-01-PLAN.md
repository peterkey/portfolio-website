---
phase: 04-copy-and-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/ExperienceTimeline.tsx
  - src/app/components/CaseStudiesSection.tsx
autonomous: true
requirements:
  - COPY-01
  - COPY-02
must_haves:
  truths:
    - "The Homelab entry is the first item in the Experience timeline"
    - "The Tesco summary opens with IT support work, not delivery driving"
    - "Every case study result field opens with a concrete metric or specific outcome, not a vague description"
  artifacts:
    - path: "src/app/components/ExperienceTimeline.tsx"
      provides: "Timeline entries in correct order with rewritten Tesco summary"
      contains: "id: 2"
    - path: "src/app/components/CaseStudiesSection.tsx"
      provides: "Case study result fields with metric-led copy"
      contains: "5 users restored"
  key_links:
    - from: "entries array index 0"
      to: "Homelab entry (id:2)"
      via: "array position"
      pattern: "id: 2"
    - from: "result field"
      to: "metric-led copy"
      via: "string value"
      pattern: "5 users restored"
---

<objective>
Rewrite the Experience timeline and Case Studies result fields so they lead with IT credibility and specific outcomes rather than vague descriptions.

Purpose: Recruiters scanning the Experience section must see infrastructure and IT work first. Case study results must open with the number or outcome — not a summary that buries the proof.
Output: ExperienceTimeline.tsx with Homelab at position 0, Tesco summary rewritten IT-first; CaseStudiesSection.tsx with all four result fields metric-led.
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-copy-and-design/04-CONTEXT.md

<interfaces>
<!-- Key structures the executor needs. Read directly from source. -->

From src/app/components/ExperienceTimeline.tsx:
```typescript
interface TimelineEntry {
  id: number;
  title: string;
  org: string;
  period: string;
  type: "work" | "education" | "project";
  summary: string;
  highlights: string[];
  tags?: string[];
  accentColor: string;
  accentBg: string;
  icon: React.ReactNode;
}

// Current array order (to be changed):
// index 0: id:1 — Tesco "Customer Delivery Driver & Informal IT Support"
// index 1: id:2 — Homelab "Home Lab — Linux Migration & Self-Hosted Infrastructure"
// index 2: id:3 — Bootcamp "Full-Stack Web Development"
// index 3: id:4 — Degree "B.Sc. Sound Technology"

// Required order:
// index 0: id:2 — Homelab (move to first)
// index 1: id:1 — Tesco (rewrite summary only)
// index 2: id:3 — Bootcamp (unchanged)
// index 3: id:4 — Degree (unchanged)
```

From src/app/components/CaseStudiesSection.tsx:
```typescript
interface CaseStudy {
  id: number;
  title: string;
  category: string;
  problem: string;
  process: string[];
  solution: string;
  result: string;   // ← this field is the target; must open with metric
  metrics: {
    resolutionTime: string;
    usersAffected: number;
    costSavings?: string;
  };
  icon: React.ReactNode;
}

// Current result fields (to be replaced):
// id:1 "Resolved all login issues without escalation..."
// id:2 "Minimized delivery downtime..."
// id:3 "Colleagues successfully adopted the new system..."
// id:4 "Successfully built and deployed..."
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder Experience entries and rewrite Tesco summary</name>
  <files>src/app/components/ExperienceTimeline.tsx</files>
  <action>
In the `entries` array, move the Homelab object (id:2) to array index 0 so it becomes the first timeline entry. The Tesco object (id:1) moves to index 1. Bootcamp (id:3) and Degree (id:4) stay at indices 2 and 3.

Do NOT change the `id` field values — they are React keys, not sort order.
Do NOT rename the Tesco entry title — keep "Customer Delivery Driver & Informal IT Support" exactly.
Do NOT remove the "Currently In Progress" callout at the bottom of the component.

Rewrite the Tesco `summary` field (currently "While in a customer-facing delivery role...") to this exact text:
"My on-site colleagues' first call for any technical problem — I provide first-line IT support in a high-volume environment, diagnosing and resolving Microsoft 365, Windows OS, Active Directory, and device issues without escalation. The delivery role is the contract; IT support is the work I actually do."

This rewrite: opens with IT support identity, states the scope concisely, positions delivery driving correctly (the job title, not the work), and keeps Peter's direct authentic tone.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Running `npm run type-check` passes. The `entries` array starts with the Homelab object (id:2 is at index 0). The Tesco summary paragraph starts with "My on-site colleagues' first call".</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite case study result fields to metric-led copy</name>
  <files>src/app/components/CaseStudiesSection.tsx</files>
  <action>
Replace the `result` string in each of the four `caseStudies` objects with the exact copy below. These are locked decisions from CONTEXT.md — use them verbatim.

id:1 (Microsoft 365 Login Resolution):
"5 users restored to full M365 access within 10 minutes each — zero escalation, zero downtime."

id:2 (Handheld Device Troubleshooting):
"3 delivery devices returned to full operation before shift end, preventing route delays for the affected drivers."

id:3 (Internal System Training Implementation):
"8 colleagues fully independent on the new system after a single training session each — eliminated recurring management requests for basic scheduling tasks."

id:4 (Home Lab Infrastructure Build — this is the fourth case study; locate it by id:4 or the homelab title):
"A production-grade home lab running multiple containerised services — fully self-hosted, zero cloud dependency, managed entirely via CLI."

Only the `result` field changes in each object. All other fields (title, category, problem, process, solution, metrics, icon) remain untouched.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Running `npm run type-check` passes. Each case study's `result` field now opens with a metric (number, outcome, or specific artifact) rather than a vague description. The first word of id:1's result is "5".</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run type-check` passes with no new errors
- In the dev server, Experience section shows Homelab as the first timeline card
- Tesco card summary begins "My on-site colleagues' first call"
- Each case study result panel opens with a concrete number or specific outcome, not "Resolved" or "Minimized"
</verification>

<success_criteria>
- Experience timeline entry order: Homelab → Tesco → Bootcamp → Degree
- Tesco entry title unchanged; summary paragraph opens with IT support identity
- All four case study result fields are metric-led (confirmed accurate per CONTEXT.md)
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-copy-and-design/04-01-SUMMARY.md` using the summary template.
</output>
