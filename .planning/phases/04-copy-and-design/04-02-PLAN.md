---
phase: 04-copy-and-design
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/AboutSection.tsx
  - src/app/components/CertificationsSection.tsx
  - src/app/components/ToolsSection.tsx
autonomous: true
requirements:
  - COPY-03
  - COPY-04
  - COPY-05
must_haves:
  truths:
    - "The About section contains no soft skills bullet list or adjective pills"
    - "The Certifications section shows only CompTIA A+ and MS-900, both with a '2026' target date instead of a progress bar"
    - "Every Tools category description is task/outcome-led with no 'Basic X' labels and no 'Professional Skills' soft skills category"
  artifacts:
    - path: "src/app/components/AboutSection.tsx"
      provides: "About section with soft skills card replaced by Currently Studying card"
      contains: "Currently Studying"
    - path: "src/app/components/CertificationsSection.tsx"
      provides: "Certifications section with two entries and targetDate field"
      contains: "targetDate"
    - path: "src/app/components/ToolsSection.tsx"
      provides: "Tools section with task-led descriptions and no Professional Skills category"
      contains: "Infrastructure & Automation"
  key_links:
    - from: "AboutSection.tsx softSkills array"
      to: "removed (no render)"
      via: "delete array + replace JSX card"
      pattern: "Currently Studying"
    - from: "CertificationsSection.tsx certifications array"
      to: "two entries with targetDate"
      via: "interface field + display JSX"
      pattern: "targetDate"
    - from: "toolCategories array"
      to: "no Professional Skills category"
      via: "category removed"
      pattern: "Infrastructure"
---

<objective>
Remove the soft skills list from the About section, reduce Certifications to the two credentialled entries with target year display, and rewrite all Tools descriptions to be task/outcome-led.

Purpose: Adjective lists (soft skills, "Basic X" labels, percentage bars) signal a weak candidate. Removing them and replacing with factual outcome-oriented content makes every section denser with proof.
Output: Three files edited — About (soft skills replaced), Certifications (reduced + target date), Tools (task descriptions + no soft-skills category).
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-copy-and-design/04-CONTEXT.md

<interfaces>
<!-- Key structures the executor needs. Read directly from source. -->

From src/app/components/AboutSection.tsx:
```typescript
// Current soft skills array (to be removed entirely):
const softSkills = [
  "Problem Solving", "Technical Documentation", "User Training",
  "Team Collaboration", "Communication", "Adaptability",
];

// Current grid structure (lines ~134-203):
// <div className="grid md:grid-cols-2 gap-6 mt-6">
//   [Technical Skills card]   [Soft Skills + Currently Learning card]  ← replace this second card
// </div>

// The "Soft Skills + Currently Learning" card (lines ~161-203) contains:
// - h3 "Soft Skills" with UserGroupIcon
// - softSkills.map() pills
// - border-t divider
// - "Currently Learning" subsection with ["CompTIA A+", "MS-900", "Kubernetes", "Ansible", "Networking+"] pills
```

From src/app/components/CertificationsSection.tsx:
```typescript
// Current interface (to be updated):
interface Certification {
  name: string;
  issuer: string;
  status: string;
  date?: string;
  progress?: number;    // ← remove from data, remove JSX references
  description: string;
  // Add:
  targetDate?: string;  // ← new field
}

// Current 5 entries — keep only:
// { name: "CompTIA A+ Certification", issuer: "CompTIA", status: "in-progress", ... }
// { name: "Microsoft 365 Fundamentals (MS-900)", issuer: "Microsoft", status: "in-progress", ... }
// Remove: Basic Active Directory Administration, ServiceNow & Jira Basics, Google IT Support Skills

// Progress bar JSX: search for ALL occurrences of cert.progress in the file — there are two:
// 1. A percentage badge near the top-right of each card
// 2. A progress bar element at the bottom of each card
// Both must be removed.
```

From src/app/components/ToolsSection.tsx:
```typescript
interface ToolCategory {
  name: string;
  icon: React.ReactNode;
  tools: string[];      // ← strip "(Basic)" suffix from any tool string
  description: string;  // ← rewrite to task/outcome-led
}

// Current 6 categories — remove "Professional Skills" (last entry) entirely:
// "Operating Systems", "Hardware & Devices", "Support Tools",
// "Development Tools", "Networking", "Professional Skills" ← remove

// Tool strings with "(Basic)" suffix to clean:
// "Active Directory (Basic)" → "Active Directory"
// "Basic Networking" → "TCP/IP Networking"
// Any other "(Basic)" occurrences
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace About section soft skills card with Currently Studying card</name>
  <files>src/app/components/AboutSection.tsx</files>
  <action>
1. Delete the `softSkills` array constant at the top of the file (lines ~23-30).

2. Remove the `UserGroupIcon` import if it becomes unused after step 3 (check — it is only used in the soft skills card).

3. Replace the entire second card in the `md:grid-cols-2` skills row (the "Soft Skills + Currently Learning" card, lines ~161-203) with a new "Currently Studying" card. The new card:
   - Uses the same outer structure: `motion.div` with the same animation props and `glow-card glass border rounded-2xl p-8` classes
   - Icon: `ServerIcon` with `bg-[#00D9FF]/10 text-[#00D9FF]` (import already present)
   - Heading: "Currently Studying"
   - Content: Two cert pills styled like the existing "Currently Learning" pills (orange border/bg variant):
     `["CompTIA A+", "MS-900 (Microsoft 365 Fundamentals)"]`
   - Below the pills, a short factual line in `text-trueAutumn-textSecondaryDark text-sm font-body` style:
     "Targeting 2026 completion — active study via Microsoft Learn and Professor Messer."
   - No soft skills list, no dividers, no adjective badges

The grid stays `md:grid-cols-2` with Technical Skills on the left and Currently Studying on the right — the layout does not change structurally.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>`npm run type-check` passes. The `softSkills` array is gone from the file. The grid row second card has heading "Currently Studying" with two cert pills. No "Soft Skills" heading or adjective pills remain in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce Certifications to two credentialled entries with target year</name>
  <files>src/app/components/CertificationsSection.tsx</files>
  <action>
1. Update the `Certification` interface: add `targetDate?: string`, keep `date?: string`, REMOVE `progress?: number`.

2. Replace the `certifications` array with exactly two entries:
```typescript
{
  name: "CompTIA A+ Certification",
  issuer: "CompTIA",
  status: "in-progress",
  targetDate: "2026",
  description: "Industry-standard hardware, software, and troubleshooting certification for IT support professionals. Studying via Professor Messer."
},
{
  name: "Microsoft 365 Fundamentals (MS-900)",
  issuer: "Microsoft",
  status: "in-progress",
  targetDate: "2026",
  description: "Covers Microsoft 365 services, cloud concepts, security, compliance, and pricing. Studying via Microsoft Learn."
}
```

3. Remove ALL JSX references to `cert.progress`. Search the entire file for `cert.progress` — there are two locations:
   - A percentage badge (likely near status icon or card header)
   - A progress bar `<div>` (likely near card bottom)
   Both must be deleted.

4. Add a target date display in place of the progress bar. Where the progress bar was, render:
```tsx
{cert.targetDate && (
  <div className="mt-4 pt-4 border-t border-[#1A3A5C]">
    <span className="font-mono text-[9px] text-[#00D9FF] tracking-widest uppercase opacity-70">
      // TARGET: {cert.targetDate}
    </span>
  </div>
)}
```
This matches the eyebrow/mono aesthetic of the design system.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>`npm run type-check` passes with no errors referencing `cert.progress`. The certifications array has exactly 2 entries. No progress bar JSX remains. Each card shows a `// TARGET: 2026` mono label.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Tools descriptions and remove Professional Skills category</name>
  <files>src/app/components/ToolsSection.tsx</files>
  <action>
1. Remove the "Professional Skills" category object entirely from `toolCategories` (last entry, name: "Professional Skills", tools: ["Problem Solving", "Documentation", "Team Collaboration", "Customer Service"]). After removal there are 5 categories.

2. Add a new "Infrastructure & Automation" category as the last entry:
```typescript
{
  name: "Infrastructure & Automation",
  icon: <ServerIcon className="h-6 w-6" />,
  tools: ["Docker", "Docker Compose", "Bash Scripting", "Linux CLI"],
  description: "Build and manage containerised services with Docker Compose — persistent volumes, isolated networks, multi-service stacks. Terminal-driven Linux administration: package management, service control, system diagnostics.",
},
```
`ServerIcon` is already imported.

3. Rewrite the `description` field for each remaining category to be task/outcome-led (not proficiency labels):
   - "Operating Systems": "Diagnose and resolve Microsoft 365 login, access, sync, and licence issues for colleagues daily. Windows OS troubleshooting: user accounts, Group Policy, system errors."
   - "Hardware & Devices": "Reset, reconfigure, and return handheld delivery devices to service — minimising shift downtime. Diagnose laptop and desktop faults: power, connectivity, peripheral failure."
   - "Support Tools": "Manage user accounts and access permissions in Active Directory. Track and resolve issues through ticketing workflows; use remote tools to diagnose problems without physical access."
   - "Development Tools": "Version control with Git across all personal projects. Built REST API integrations, automated repetitive tasks with Bash scripts, and maintain infrastructure config as code."
   - "Networking": "Configure and troubleshoot TCP/IP networks: DHCP, DNS, routing, VPN. Manage Docker network isolation — bridge, host, and custom networks for containerised services."

4. Remove "(Basic)" from all tool pill strings:
   - "Active Directory (Basic)" → "Active Directory"
   - "Basic Networking" → "TCP/IP Networking" (or simply remove from "Networking" category since it's covered in description)
   - Any other "(Basic)" occurrences

5. Remove `ShieldCheckIcon` from imports if it was only used in the Professional Skills category (check before removing).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>`npm run type-check` passes. `toolCategories` has 5 entries (no "Professional Skills"). No tool string contains "(Basic)". Each description is a sentence describing real tasks. "Infrastructure & Automation" category exists with Docker/Bash/Linux tools.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `npm run type-check` passes
- About section: grid still shows two columns in the skills row; right column is "Currently Studying" with two cert pills, no adjective list
- Certifications section: two cards only (CompTIA A+, MS-900); each shows `// TARGET: 2026`; no progress bars
- Tools section: 5 category cards; descriptions are sentences about real tasks; no pill reads "Active Directory (Basic)" or "Basic Networking"; no "Professional Skills" card
</verification>

<success_criteria>
- No soft skills list in About (no "Communication", "Adaptability" adjective pills)
- Certifications reduced from 5 to 2; progress bars gone; target year shown
- Tools Professional Skills category removed; all descriptions task-led; no "(Basic)" labels
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-copy-and-design/04-02-SUMMARY.md` using the summary template.
</output>
