---
phase: 02-hero-hook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/HeroSection.tsx
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04

must_haves:
  truths:
    - "The role title is the single static string 'IT Support Specialist' — no cycling, no deletion animation"
    - "The value proposition reads 'I keep businesses running by resolving IT issues before they become problems'"
    - "The stats strip shows three cards: '8+ / YRS EXPERIENCE', 'FCR-FOCUSED' (no label), '120+ / USERS / WEEK'"
    - "All hero content is visible immediately on page load — no content hidden behind animation delays"
    - "The Download CV button links to /peter-key-cv.pdf, not /cv.txt"
    - "react-type-animation is not present in package.json dependencies"
  artifacts:
    - path: "src/app/components/HeroSection.tsx"
      provides: "Rewritten hero left column — static title, outcome value prop, updated stats, zero delays"
      excludes: "TypeAnimation import, rotating sequence, staggered delays > 0"
  key_links:
    - from: "motion.div wrappers (left column)"
      to: "delay: 0"
      via: "transition prop"
      pattern: "delay: 0"
    - from: "role title motion.div"
      to: "static span"
      via: "span element with inline color style"
      pattern: "<span style.*IT Support Specialist"
    - from: "CV download anchor"
      to: "/peter-key-cv.pdf"
      via: "href attribute"
      pattern: "href=\"/peter-key-cv.pdf\""
---

<objective>
Rewrite the HeroSection left column to communicate Peter's role and value within 5 seconds of landing: static role title, outcome-focused value proposition, reframed stats strip, and all content visible on load with no animation delay.

Purpose: Recruiters scan fast. The current hero hides the CTA behind a 0.9s delay and cycles through role titles that undermine clarity. Every change in this plan removes friction between the visitor and the decision to contact.
Output: Updated HeroSection.tsx with all four HERO requirements satisfied; react-type-animation package removed.
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-hook/02-CONTEXT.md
@.planning/phases/02-hero-hook/02-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts extracted from the current HeroSection.tsx that the executor must preserve. -->

Stats data array (current — lines 332–335):
```tsx
{ value: "8+",   label: "YRS EXPERIENCE" },
{ value: "20+",  label: "STAFF SUPPORTED" },
{ value: "120+", label: "USERS / WEEK" },
```

Stats card render (current — lines 336–344): uses `key={stat.label}` — must change to `key={stat.label || stat.value}` because the FCR-FOCUSED card has an empty label.

Conditional label render pattern required after update:
```tsx
{stat.label && (
  <div className="font-mono text-[0.55rem] text-[#4A6A8A] tracking-widest mt-0.5">{stat.label}</div>
)}
```

TypeAnimation block (current — lines 296–309): remove entirely; replace with:
```tsx
<span style={{ color: "#00D9FF" }}>IT Support Specialist</span>
```

Description paragraph text (current — lines 320–322): replace the string content only; keep all className attributes unchanged.

CV download href (current — line 355): `href="/cv.txt"` — MUST become `href="/peter-key-cv.pdf"`.
Download attribute (line 356): `download="Peter_Williams-Key_CV.pdf"` — keep as-is.

Left column motion.div delays to zero (all delay values, keep duration unchanged):
- Status badge: delay 0.1 → 0
- HOSTNAME label: delay 0.2 → 0
- h1: delay 0.3 → 0
- Role title div: delay 0.45 → 0
- Description: delay 0.6 → 0
- Stats strip: delay 0.75 → 0
- CTA buttons: delay 0.9 → 0
- Social row: delay 1.05 → 0
- Scroll indicator (delay 1.6): leave unchanged — below the fold, decorative
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite HeroSection left column</name>
  <files>src/app/components/HeroSection.tsx</files>
  <action>
Edit src/app/components/HeroSection.tsx with the following surgical changes — all in one pass:

1. **Remove import (line 3):** Delete `import { TypeAnimation } from "react-type-animation";` entirely.

2. **Replace TypeAnimation JSX (lines 296–309):** Remove the entire `<TypeAnimation ... />` block. Replace with:
   ```tsx
   <span style={{ color: "#00D9FF" }}>IT Support Specialist</span>
   ```
   The parent `motion.div` and its `min-h-[2rem]` class stay unchanged.

3. **Replace description text (lines 320–322):** Change the paragraph text content to:
   `I keep businesses running by resolving IT issues before they become problems.`
   Keep the `className` on the `motion.p` exactly as-is.

4. **Update stats array (lines 332–335):**
   ```tsx
   { value: "8+",          label: "YRS EXPERIENCE" },
   { value: "FCR-FOCUSED", label: "" },
   { value: "120+",        label: "USERS / WEEK" },
   ```
   Change `key={stat.label}` to `key={stat.label || stat.value}`.
   Add conditional label render: wrap the label `<div>` in `{stat.label && (...)}`  so the FCR-FOCUSED card renders no label row.

5. **Fix CV download href (line 355):** Change `href="/cv.txt"` to `href="/peter-key-cv.pdf"`. The `download` attribute stays as `"Peter_Williams-Key_CV.pdf"`.

6. **Zero all left-column animation delays:** For each `motion.div` and `motion.p` in the left column (NOT the scroll indicator, NOT the right column), set `delay` to `0` in their `transition` prop. Keep `duration` values unchanged:
   - Status badge: `delay: 0.1` → `delay: 0`
   - HOSTNAME label: `delay: 0.2` → `delay: 0`
   - h1: `delay: 0.3` → `delay: 0`
   - Role title div: `delay: 0.45` → `delay: 0`
   - Description: `delay: 0.6` → `delay: 0`
   - Stats strip: `delay: 0.75` → `delay: 0`
   - CTA buttons: `delay: 0.9` → `delay: 0`
   - Social row: `delay: 1.05` → `delay: 0`
   - Scroll indicator delay 1.6: LEAVE UNCHANGED

   The outer left column wrapper already has no delay — leave it.

   Do NOT change anything in the right column (NetworkDiagram, panel header, status strip).

After writing the file, uninstall the now-unused package:
```bash
cd /home/peter/Documents/portfolio-website && npm uninstall react-type-animation
```

Then run the type-check to confirm no TypeScript errors:
```bash
cd /home/peter/Documents/portfolio-website && npm run type-check
```
  </action>
  <verify>
    <automated>cd /home/peter/Documents/portfolio-website && npm run type-check</automated>
  </verify>
  <done>
    - `npm run type-check` exits with code 0 (no TypeScript errors)
    - `react-type-animation` is absent from package.json dependencies
    - No `TypeAnimation` import remains in HeroSection.tsx
    - Role title is `<span style={{ color: "#00D9FF" }}>IT Support Specialist</span>`
    - Description text reads "I keep businesses running by resolving IT issues before they become problems."
    - Stats array has FCR-FOCUSED with empty label; key is `stat.label || stat.value`
    - CV href is `/peter-key-cv.pdf`
    - All left-column motion.div delay values are 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification in browser</name>
  <what-built>
    HeroSection rewritten: static role title, outcome value proposition, reframed stats, all content visible instantly on load, CV link fixed. react-type-animation removed.
  </what-built>
  <how-to-verify>
    Run `npm run dev` from your terminal, then open http://localhost:3000 in a browser.

    Check each of the following:

    1. **Role title (HERO-01):** The title area below "Peter Williams-Key" shows "IT Support Specialist" as a static string. No typing animation, no deletion, no cycling.

    2. **Value proposition (HERO-02):** Below the title, the line reads: "I keep businesses running by resolving IT issues before they become problems." — not the old "Customer-focused IT professional..." paragraph.

    3. **Stats strip (HERO-03):** Three stat cards are visible. First: "8+" with "YRS EXPERIENCE". Second: "FCR-FOCUSED" with no label below it. Third: "120+" with "USERS / WEEK". The FCR-FOCUSED card should look intentional, not broken.

    4. **Instant load (HERO-04):** Hard-refresh the page (Ctrl+Shift+R). All hero content — status badge, name, title, value prop, stats, and both CTA buttons — should appear immediately with no staggered delay. The entrance slide/fade animation is fine; what should be gone is any delay where content is invisible for a moment before appearing.

    5. **Download CV button:** Click "Download CV". Confirm it downloads a real PDF named "Peter-Key-CV.pdf" (not cv.txt).
  </how-to-verify>
  <action>Human verifies the five visual checks listed in how-to-verify. No code changes required — this task pauses execution for user confirmation.</action>
  <verify>
    <automated>MISSING — visual-only; no automated check possible without browser testing infra</automated>
  </verify>
  <done>User types "approved" after confirming all five checks pass in the browser.</done>
  <resume-signal>Type "approved" if all five checks pass, or describe any issue to fix.</resume-signal>
</task>

</tasks>

<verification>
TypeScript clean: `npm run type-check` passes with 0 errors after Task 1.
Visual verification: All five manual checks in Task 2 pass in the browser.
</verification>

<success_criteria>
- `npm run type-check` exits 0
- No TypeAnimation import or usage in HeroSection.tsx
- react-type-animation absent from package.json
- Hero shows static "IT Support Specialist" title
- Value proposition copy is outcome-focused
- Stats show FCR-FOCUSED, not "20+ STAFF SUPPORTED"
- All left-column content visible on hard-refresh with no animation delay
- Download CV links to /peter-key-cv.pdf
</success_criteria>

<output>
After completion, create `.planning/phases/02-hero-hook/02-01-SUMMARY.md` using the summary template.
</output>
