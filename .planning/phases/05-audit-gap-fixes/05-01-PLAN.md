---
phase: 05-audit-gap-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/HeroSection.tsx
  - src/app/components/Navbar.tsx
  - src/app/components/Footer.tsx
autonomous: true
requirements:
  - FIX-02
  - DESIGN-03
must_haves:
  truths:
    - "The CV download filename in HeroSection matches Peter-Key-CV.pdf (same as Navbar)"
    - "HeroSection renders all elements in their final state immediately when OS prefers-reduced-motion is enabled — no entrance animations, no bounce loop"
    - "Navbar does not hide on scroll when OS prefers-reduced-motion is enabled"
    - "Footer navigation includes Case Studies and Certifications links in the correct order"
  artifacts:
    - path: "src/app/components/HeroSection.tsx"
      provides: "Fixed filename + useReducedMotion on all 14 motion elements"
      contains: "Peter-Key-CV.pdf"
    - path: "src/app/components/Navbar.tsx"
      provides: "useReducedMotion guarding motion.nav, motion.button, motion.div"
      contains: "shouldReduceMotion"
    - path: "src/app/components/Footer.tsx"
      provides: "Updated navLinks with Case Studies and Certifications"
      contains: "case-studies"
  key_links:
    - from: "HeroSection.tsx motion.a download attribute"
      to: "public/peter-key-cv.pdf"
      via: "download attribute value"
      pattern: "download=\"Peter-Key-CV\\.pdf\""
    - from: "useReducedMotion() hook"
      to: "motion.* initial/animate/transition props"
      via: "shouldReduceMotion ternary guard"
      pattern: "shouldReduceMotion"
    - from: "Footer navLinks array"
      to: "rendered footer nav anchors"
      via: "navLinks.map()"
      pattern: "case-studies"
---

<objective>
Close three specific defects found in the v1.0 audit: CV filename mismatch in HeroSection, missing useReducedMotion guards in HeroSection and Navbar, and stale navLinks in Footer.

Purpose: These are live defects — a recruiter downloading the CV gets a mismatched filename, users with prefers-reduced-motion get a broken experience, and the Footer nav is missing sections added in Phase 3.
Output: Three surgically edited files. No new components, no new dependencies, no scope beyond the three identified defects.
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-audit-gap-fixes/05-CONTEXT.md
@.planning/phases/05-audit-gap-fixes/05-RESEARCH.md

<interfaces>
<!-- Exact current state of each target file. Read before editing. -->

HeroSection.tsx — current motion import (line 3):
```typescript
import { motion } from "framer-motion";
```
Change to: `import { motion, useReducedMotion } from "framer-motion";`

HeroSection.tsx — filename defect (line 342):
```tsx
download="Peter_Williams-Key_CV.pdf"
```
Change to: `download="Peter-Key-CV.pdf"`

HeroSection.tsx — complete motion.* inventory (14 elements, in render order):
1. motion.div — left column wrapper: `initial={{ opacity: 0, x: -40 }}` `animate={{ opacity: 1, x: 0 }}` `transition={{ duration: 0.9, ease: "easeOut" }}`
2. motion.div — status badge: `initial={{ opacity: 0, y: 16 }}` `animate={{ opacity: 1, y: 0 }}` `transition={{ duration: 0.6, delay: 0 }}`
3. motion.p — hostname label: `initial={{ opacity: 0, y: 16 }}` `animate={{ opacity: 1, y: 0 }}` `transition={{ duration: 0.6, delay: 0 }}`
4. motion.h1 — name heading: `initial={{ opacity: 0, y: 24 }}` `animate={{ opacity: 1, y: 0 }}` `transition={{ duration: 0.8, delay: 0 }}`
5. motion.div — role title: `initial={{ opacity: 0, y: 16 }}` `animate={{ opacity: 1, y: 0 }}` `transition={{ duration: 0.7, delay: 0 }}`
6. motion.p — description: `initial={{ opacity: 0, y: 14 }}` `animate={{ opacity: 1, y: 0 }}` `transition={{ duration: 0.7, delay: 0 }}`
7. motion.div — metrics strip: `initial={{ opacity: 0, y: 14 }}` `animate={{ opacity: 1, y: 0 }}` `transition={{ duration: 0.7, delay: 0 }}`
8. motion.div — CTA buttons row: `initial={{ opacity: 0, y: 14 }}` `animate={{ opacity: 1, y: 0 }}` `transition={{ duration: 0.7, delay: 0 }}`
9. motion.a — Download CV button: `whileHover={{ scale: 1.03 }}` `whileTap={{ scale: 0.97 }}`
10. motion.a — Get in Touch button: `whileHover={{ scale: 1.03 }}` `whileTap={{ scale: 0.97 }}`
11. motion.div — social row: `initial={{ opacity: 0 }}` `animate={{ opacity: 1 }}` `transition={{ duration: 0.7, delay: 0 }}`
12. motion.div — right column diagram wrapper: `initial={{ opacity: 0, x: 40 }}` `animate={{ opacity: 1, x: 0 }}` `transition={{ duration: 1.0, ease: "easeOut", delay: 0.15 }}`
13. motion.div — scroll indicator outer: `initial={{ opacity: 0 }}` `animate={{ opacity: 1 }}` `transition={{ duration: 1, delay: 1.6 }}`
14. motion.div — scroll indicator bounce inner: `animate={{ y: [0, 8, 0] }}` `transition={{ duration: 1.8, repeat: Infinity }}`

Navbar.tsx — current motion import (line 3):
```typescript
import { motion } from "framer-motion";
```
Change to: `import { motion, useReducedMotion } from "framer-motion";`

Navbar.tsx — complete motion.* inventory (4 elements):
1. motion.div — mobile overlay bottom bar (inside `{isOpen && ...}` block): `initial={{ opacity: 0 }}` `animate={{ opacity: 1 }}` `transition={{ delay: 0.45 }}`
2. motion.nav — primary navbar: `animate={{ y: hidden ? "-100%" : "0%" }}` `transition={{ duration: 0.35, ease: [0.22, 1, 0.36, 1] }}`
3. motion.button — Download CV (desktop): `whileHover={{ scale: 1.04 }}` `whileTap={{ scale: 0.97 }}`
4. motion.div — gradient separator: `animate={{ opacity: scrolled ? 1 : 0 }}` `transition={{ duration: 0.3 }}`

Footer.tsx — current stale navLinks (lines 1-7):
```typescript
const navLinks = [
  { title: "About", path: "#about" },
  { title: "Skills", path: "#skills" },
  { title: "Experience", path: "#experience" },
  { title: "Projects", path: "#projects" },
  { title: "Contact", path: "#contact" },
];
```

Established guard pattern (copy verbatim from research — confirmed across 6 existing components):
```typescript
// Entrance animations (initial/animate):
initial={shouldReduceMotion ? false : { opacity: 0, y: 20 }}
animate={shouldReduceMotion ? undefined : { opacity: 1, y: 0 }}
transition={shouldReduceMotion ? undefined : { duration: 0.6 }}

// Loop suppression (bounce):
animate={shouldReduceMotion ? undefined : { y: [0, 8, 0] }}
transition={shouldReduceMotion ? undefined : { duration: 1.8, repeat: Infinity }}

// whileHover/whileTap:
whileHover={shouldReduceMotion ? undefined : { scale: 1.03 }}
whileTap={shouldReduceMotion ? undefined : { scale: 0.97 }}

// Navbar scroll-hide (keep motion.nav — do NOT swap to plain nav):
animate={{ y: shouldReduceMotion ? "0%" : (hidden ? "-100%" : "0%") }}
transition={shouldReduceMotion ? undefined : { duration: 0.35, ease: [0.22, 1, 0.36, 1] }}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CV filename and add useReducedMotion to HeroSection</name>
  <files>src/app/components/HeroSection.tsx</files>
  <action>
Read HeroSection.tsx in full before editing. Make two changes:

**Change 1 — Fix filename (FIX-02):**
On line 342, change the download attribute:
- From: `download="Peter_Williams-Key_CV.pdf"`
- To: `download="Peter-Key-CV.pdf"`

**Change 2 — Add useReducedMotion (DESIGN-03):**

Step 1: Update the framer-motion import on line 3:
- From: `import { motion } from "framer-motion";`
- To: `import { motion, useReducedMotion } from "framer-motion";`

Step 2: Add `const shouldReduceMotion = useReducedMotion();` as the first line inside the `HeroSection` component function body (immediately after `const HeroSection = () => {`).

Step 3: Gate all 14 motion elements using the `shouldReduceMotion` ternary pattern. For each element, preserve all existing prop values — only wrap them in the conditional. Use the exact patterns from the `<interfaces>` block above.

Elements 1–8 and 11–13 (entrance animations using initial/animate/transition):
```tsx
initial={shouldReduceMotion ? false : { /* existing values */ }}
animate={shouldReduceMotion ? undefined : { /* existing values */ }}
transition={shouldReduceMotion ? undefined : { /* existing values */ }}
```

Element 14 — scroll indicator bounce inner (perpetual loop):
```tsx
animate={shouldReduceMotion ? undefined : { y: [0, 8, 0] }}
transition={shouldReduceMotion ? undefined : { duration: 1.8, repeat: Infinity }}
```

Elements 9 and 10 — motion.a CTA buttons (whileHover/whileTap only, no initial/animate):
```tsx
whileHover={shouldReduceMotion ? undefined : { scale: 1.03 }}
whileTap={shouldReduceMotion ? undefined : { scale: 0.97 }}
```

Element 12 — right column wrapper has `delay: 0.15` — preserve that in the transition guard:
```tsx
transition={shouldReduceMotion ? undefined : { duration: 1.0, ease: "easeOut", delay: 0.15 }}
```

Element 13 — scroll indicator outer has `delay: 1.6` — preserve that:
```tsx
transition={shouldReduceMotion ? undefined : { duration: 1, delay: 1.6 }}
```

Do not touch the SVG NetworkDiagram component — its CSS `<animateTransform>` and `<animate>` elements are SVG-native, not Framer Motion, and are out of scope for this plan.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>`npm run type-check` passes. HeroSection.tsx line containing the download attribute reads `download="Peter-Key-CV.pdf"`. The file imports `useReducedMotion` from framer-motion. The `shouldReduceMotion` variable is declared at component top level. All 14 motion elements have gated animation props.</done>
</task>

<task type="auto">
  <name>Task 2: Add useReducedMotion to Navbar</name>
  <files>src/app/components/Navbar.tsx</files>
  <action>
Read Navbar.tsx in full before editing.

Step 1: Update the framer-motion import on line 3:
- From: `import { motion } from "framer-motion";`
- To: `import { motion, useReducedMotion } from "framer-motion";`

Step 2: Add `const shouldReduceMotion = useReducedMotion();` as the first line inside the `Navbar` component function body (immediately after `const Navbar = () => {`), before the existing `useState` variables.

Step 3: Gate all 4 motion elements:

**motion.nav — scroll-hide (keep as motion.nav, do NOT swap to plain nav):**
```tsx
animate={{ y: shouldReduceMotion ? "0%" : (hidden ? "-100%" : "0%") }}
transition={shouldReduceMotion ? undefined : { duration: 0.35, ease: [0.22, 1, 0.36, 1] }}
```

**motion.button — Download CV (desktop):**
```tsx
whileHover={shouldReduceMotion ? undefined : { scale: 1.04 }}
whileTap={shouldReduceMotion ? undefined : { scale: 0.97 }}
```

**motion.div — mobile overlay bottom bar (inside the `{isOpen && ...}` conditional block):**
```tsx
initial={shouldReduceMotion ? false : { opacity: 0 }}
animate={shouldReduceMotion ? undefined : { opacity: 1 }}
transition={shouldReduceMotion ? undefined : { delay: 0.45 }}
```

**motion.div — gradient separator (opacity-only, no spatial motion):**
```tsx
animate={{ opacity: shouldReduceMotion ? (scrolled ? 1 : 0) : (scrolled ? 1 : 0) }}
transition={shouldReduceMotion ? undefined : { duration: 0.3 }}
```
Note: The gradient separator's animate value is the same whether reduced-motion is on or off (it's opacity only, not vestibular), so gate only the transition. Alternatively, simplify to leave both as-is since the effect has no spatial motion — either approach is correct per research. The safe default: gate the transition prop, leave animate unchanged.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>`npm run type-check` passes. Navbar.tsx imports `useReducedMotion`. The hook is called at component top level. The motion.nav animate prop uses the `shouldReduceMotion ? "0%" : (hidden ? "-100%" : "0%")` pattern. motion.button whileHover/whileTap are gated.</done>
</task>

<task type="auto">
  <name>Task 3: Update Footer navLinks to match current page sections</name>
  <files>src/app/components/Footer.tsx</files>
  <action>
Read Footer.tsx before editing.

Replace the entire `navLinks` array at the top of the file (lines 1-7) with:
```typescript
const navLinks = [
  { title: "About",          path: "#about" },
  { title: "Experience",     path: "#experience" },
  { title: "Case Studies",   path: "#case-studies" },
  { title: "Projects",       path: "#projects" },
  { title: "Certifications", path: "#certifications" },
  { title: "Contact",        path: "#contact" },
];
```

Note: The old array had `{ title: "Skills", path: "#skills" }` — remove it. Skills is not in the Navbar nav and is not included per CONTEXT.md. The `num` field is Navbar-only — do not add it here. No other changes to Footer.tsx.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>`npm run type-check` passes. Footer.tsx navLinks array contains exactly 6 entries: About, Experience, Case Studies, Projects, Certifications, Contact. The `#case-studies` and `#certifications` hrefs are present. The `#skills` entry is removed.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `npm run type-check` passes with no new errors
- HeroSection.tsx: `download="Peter-Key-CV.pdf"` (no underscores)
- HeroSection.tsx: imports `useReducedMotion`; `const shouldReduceMotion = useReducedMotion()` at component top
- Navbar.tsx: imports `useReducedMotion`; `const shouldReduceMotion = useReducedMotion()` at component top; motion.nav animate uses the three-way ternary
- Footer.tsx: navLinks has 6 entries; `#case-studies` and `#certifications` are present; `#skills` is absent

Manual check (with OS prefers-reduced-motion enabled):
- Load page: HeroSection renders with no fade/slide entrance animations and scroll indicator is static
- Scroll down: Navbar stays pinned at top — does not hide
- Footer: Case Studies and Certifications links are visible and anchor correctly
</verification>

<success_criteria>
- FIX-02: `download="Peter-Key-CV.pdf"` in HeroSection.tsx matches Navbar's `a.download = "Peter-Key-CV.pdf"` exactly
- DESIGN-03: All motion.* elements in HeroSection (14) and Navbar (4) are gated with `shouldReduceMotion` ternaries
- Footer navLinks: 6 entries matching current page sections; `#case-studies` and `#certifications` present
- TypeScript compilation succeeds (`npm run type-check` passes)
</success_criteria>

<output>
After completion, create `.planning/phases/05-audit-gap-fixes/05-01-SUMMARY.md` using the summary template.
</output>
