---
phase: 03-page-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/components/Navbar.tsx
  - src/app/components/CaseStudiesSection.tsx
  - src/app/components/ProjectSection.tsx
autonomous: false
requirements:
  - STRUCT-01
  - STRUCT-02
  - STRUCT-03
  - STRUCT-04

must_haves:
  truths:
    - "Case Studies section appears immediately after Experience in DOM scroll order (before Projects, Tools, Certifications)"
    - "No TestimonialsSection renders on the page"
    - "No AchievementsSection renders as a standalone section"
    - "Section backgrounds strictly alternate dark/cardDark: About=cardDark, Experience=dark, CaseStudies=cardDark, Projects=dark, Tools=cardDark, Certifications=dark, Contact=cardDark"
    - "Navbar has exactly 7 links with Case Studies at num 03 and #case-studies active tracking working"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Section render order — CaseStudies before Projects, Achievements and Testimonials removed"
      contains: "CaseStudiesSection />"
    - path: "src/app/components/Navbar.tsx"
      provides: "navLinks array with Case Studies at position 03"
      contains: "case-studies"
    - path: "src/app/components/CaseStudiesSection.tsx"
      provides: "cardDark background for position 4 in scroll order"
      contains: "bg-trueAutumn-cardDark"
    - path: "src/app/components/ProjectSection.tsx"
      provides: "dark background for position 5 in scroll order"
      contains: "bg-trueAutumn-dark"
  key_links:
    - from: "src/app/components/Navbar.tsx"
      to: "src/app/components/CaseStudiesSection.tsx"
      via: "navLinks path #case-studies → id='case-studies' on section element"
      pattern: "case-studies"
    - from: "src/app/page.tsx"
      to: "removed sections"
      via: "imports and JSX both removed together"
      pattern: "AchievementsSection|TestimonialsSection"
---

<objective>
Reorder page sections so Case Studies surfaces before Projects in the scroll journey, remove the two placeholder sections (Testimonials and Achievements) that hurt credibility, update the Navbar to reflect the new structure, and fix the background alternation on the two repositioned sections.

Purpose: Credibility evidence (Case Studies) must appear early. Placeholder sections with no real content damage trust.
Output: `page.tsx` with 8 sections in the correct order; `Navbar.tsx` with 7 links correctly numbered; `CaseStudiesSection.tsx` and `ProjectSection.tsx` with swapped backgrounds.
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-page-structure/03-CONTEXT.md
@.planning/phases/03-page-structure/03-RESEARCH.md

<interfaces>
<!-- Key facts confirmed by direct codebase read. Use these — no exploration needed. -->

Current page.tsx section order (lines 19–28):
```tsx
<HeroSection />
<AboutSection />
<ExperienceTimeline />
<ProjectSection />        // currently position 4
<CaseStudiesSection />    // currently position 5
<ToolsSection />
<CertificationsSection />
<AchievementsSection />   // REMOVE
<TestimonialsSection />   // REMOVE
<EnhancedContactSection />
```

Current page.tsx imports include (to be removed):
- `import AchievementsSection from "./components/AchievementsSection";`
- `import TestimonialsSection from "./components/TestimonialsSection";`
- `import Image from "next/image";` (unused — also remove)

Current navLinks (Navbar.tsx lines 9–17):
```ts
const navLinks = [
  { title: "About",          path: "#about",          num: "01" },
  { title: "Experience",     path: "#experience",     num: "02" },
  { title: "Projects",       path: "#projects",       num: "03" },
  { title: "Skills",         path: "#skills",         num: "04" },
  { title: "Certifications", path: "#certifications", num: "05" },
  { title: "Achievements",   path: "#achievements",   num: "06" },
  { title: "Contact",        path: "#contact",        num: "07" },
];
```

Background swap targets (outermost `<section>` element only):
- CaseStudiesSection.tsx line 119: `bg-trueAutumn-dark` → `bg-trueAutumn-cardDark`
- ProjectSection.tsx line 63: `bg-trueAutumn-cardDark` → `bg-trueAutumn-dark`

Contact section already correct (cardDark at line 57) — no action needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder sections and remove placeholders in page.tsx</name>
  <files>src/app/page.tsx</files>
  <action>
    Edit `src/app/page.tsx` to produce the new section order and clean up dead imports.

    TARGET import block (top of file) — remove three imports, keep everything else:
    - REMOVE: `import AchievementsSection from "./components/AchievementsSection";`
    - REMOVE: `import TestimonialsSection from "./components/TestimonialsSection";`
    - REMOVE: `import Image from "next/image";` (unused)

    TARGET JSX order inside `<main>`:
    ```tsx
    <Navbar />
    <HeroSection />
    <AboutSection />
    <ExperienceTimeline />
    <CaseStudiesSection />
    <ProjectSection />
    <ToolsSection />
    <CertificationsSection />
    <EnhancedContactSection />
    <Footer />
    ```

    Do NOT delete the component files `AchievementsSection.tsx` or `TestimonialsSection.tsx` — remove only the import lines and JSX elements from `page.tsx`.

    Both the import statement and the JSX element must be removed in the same edit to avoid stale import warnings.
  </action>
  <verify>
    Run `npm run type-check` from the project root. Confirm zero TypeScript errors related to this file.
    Visually confirm: no mention of `AchievementsSection` or `TestimonialsSection` anywhere in the updated `page.tsx`.
  </verify>
  <done>
    `page.tsx` renders exactly 8 sections in this order: Hero, About, Experience, CaseStudies, Projects, Tools, Certifications, Contact. No AchievementsSection or TestimonialsSection import or JSX element present. No unused Image import.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Navbar navLinks array to reflect new structure</name>
  <files>src/app/components/Navbar.tsx</files>
  <action>
    Replace the `navLinks` constant at lines 9–17 with the new 7-entry array:

    ```ts
    const navLinks = [
      { title: "About",          path: "#about",          num: "01" },
      { title: "Experience",     path: "#experience",     num: "02" },
      { title: "Case Studies",   path: "#case-studies",   num: "03" },
      { title: "Projects",       path: "#projects",       num: "04" },
      { title: "Skills",         path: "#skills",         num: "05" },
      { title: "Certifications", path: "#certifications", num: "06" },
      { title: "Contact",        path: "#contact",        num: "07" },
    ];
    ```

    Key changes:
    - "Achievements" (num: "06", path: "#achievements") is REPLACED by "Case Studies" (num: "03", path: "#case-studies")
    - "Projects", "Skills", "Certifications", "Contact" are renumbered: 04, 05, 06, 07

    Do not touch any other part of Navbar.tsx. The IntersectionObserver at lines 99–112 reads `path.slice(1)` from this array — the new `#case-studies` entry will automatically enable active tracking once `CaseStudiesSection` has `id="case-studies"` (it already does).
  </action>
  <verify>
    Visually inspect `Navbar.tsx` — confirm 7 entries, no "Achievements" entry, "Case Studies" at num "03".
    Run `npm run type-check` to confirm no TypeScript errors.
  </verify>
  <done>
    `navLinks` has exactly 7 entries. "Case Studies" appears at num "03" with path "#case-studies". "Achievements" does not appear. Numbers run sequentially 01–07 with no gaps.
  </done>
</task>

<task type="auto">
  <name>Task 3: Swap background classes on CaseStudiesSection and ProjectSection</name>
  <files>src/app/components/CaseStudiesSection.tsx, src/app/components/ProjectSection.tsx</files>
  <action>
    Two single-token swaps on the outermost `<section>` element in each component. Target the exact lines confirmed by research.

    **CaseStudiesSection.tsx line 119:**
    Change `bg-trueAutumn-dark` to `bg-trueAutumn-cardDark` on the outermost `<section>` element.

    Before:
    ```tsx
    <section id="case-studies" className="py-20 sm:py-28 px-4 bg-trueAutumn-dark relative overflow-hidden">
    ```
    After:
    ```tsx
    <section id="case-studies" className="py-20 sm:py-28 px-4 bg-trueAutumn-cardDark relative overflow-hidden">
    ```

    **ProjectSection.tsx line 63:**
    Change `bg-trueAutumn-cardDark` to `bg-trueAutumn-dark` on the outermost `<section>` element.

    Before:
    ```tsx
    <section id='projects' className='py-20 sm:py-28 bg-trueAutumn-cardDark relative overflow-hidden' aria-labelledby="projects-heading">
    ```
    After:
    ```tsx
    <section id='projects' className='py-20 sm:py-28 bg-trueAutumn-dark relative overflow-hidden' aria-labelledby="projects-heading">
    ```

    Only swap the background token on the outermost `<section>`. Do not change any inner `<div>` elements or any other class values.
  </action>
  <verify>
    Grep both files to confirm the swapped values:
    - `CaseStudiesSection.tsx` contains `bg-trueAutumn-cardDark` and NOT `bg-trueAutumn-dark` on the section element
    - `ProjectSection.tsx` contains `bg-trueAutumn-dark` and NOT `bg-trueAutumn-cardDark` on the section element
  </verify>
  <done>
    CaseStudiesSection outermost section has `bg-trueAutumn-cardDark`. ProjectSection outermost section has `bg-trueAutumn-dark`. The strict dark/cardDark alternation from About through Contact is correct for the new section positions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of page structure</name>
  <files>none</files>
  <action>No automated action — human verifies the three structural edits in the browser.</action>
  <verify>Human confirms all four checks below pass.</verify>
  <done>Human types "approved" confirming scroll order, background alternation, navbar links, and active tracking all look correct.</done>
  <what-built>
    Three structural edits:
    1. page.tsx — CaseStudies moved before Projects; Achievements and Testimonials removed
    2. Navbar.tsx — 7 links with Case Studies at 03, renumbered through 07
    3. Background classes swapped on CaseStudiesSection and ProjectSection
  </what-built>
  <how-to-verify>
    Start the dev server (`npm run dev`) and open http://localhost:3000 in your browser.

    1. **Scroll order**: Scroll from top — confirm the sequence is: Hero, About, Experience, Case Studies, Projects, Tools, Certifications, Contact. No Achievements or Testimonials section visible anywhere.

    2. **Background alternation**: As you scroll past the Hero, confirm the bands alternate correctly:
       - About: darker card tone (#0A1628)
       - Experience: deepest dark (#060D18)
       - Case Studies: darker card tone (#0A1628) — previously same dark as Experience
       - Projects: deepest dark (#060D18) — previously same card tone as Case Studies
       - Tools: darker card tone
       - Certifications: deepest dark
       - Contact: darker card tone

    3. **Navbar links**: View the desktop nav or open the hamburger menu. Confirm 7 links numbered 01–07. Link 03 reads "Case Studies". No "Achievements" link present.

    4. **Active tracking**: Click "03 Case Studies" in the nav — confirm it smooth-scrolls to the Case Studies section and the link highlights as active when that section is in view.
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe what looks wrong</resume-signal>
</task>

</tasks>

<verification>
TypeScript compilation must pass with zero errors on the modified files:
```bash
npm run type-check
```

All four STRUCT requirements verified visually in browser:
- STRUCT-01: CaseStudiesSection renders before ToolsSection and CertificationsSection in DOM
- STRUCT-02: No TestimonialsSection rendered
- STRUCT-03: No AchievementsSection rendered
- STRUCT-04: Background alternation visually correct after reorder
</verification>

<success_criteria>
- `page.tsx` renders exactly 8 sections in the order: Hero, About, Experience, CaseStudies, Projects, Tools, Certifications, Contact
- `AchievementsSection` and `TestimonialsSection` are absent from both imports and JSX in `page.tsx`
- `navLinks` has 7 entries, "Case Studies" at num "03", no "Achievements" entry, sequential numbering 01–07
- `CaseStudiesSection.tsx` outermost section uses `bg-trueAutumn-cardDark`
- `ProjectSection.tsx` outermost section uses `bg-trueAutumn-dark`
- `npm run type-check` exits with zero errors
- Human checkpoint: all four visual checks approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-structure/03-01-SUMMARY.md` using the template at `@/home/peter/.claude/get-shit-done/templates/summary.md`.
</output>
