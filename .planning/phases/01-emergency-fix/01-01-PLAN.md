---
phase: 01-emergency-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/peter-key-cv.pdf
  - src/app/components/Navbar.tsx
autonomous: false
requirements:
  - FIX-01
  - FIX-02

must_haves:
  truths:
    - "Clicking 'Download CV' triggers a file download that opens correctly in a PDF viewer"
    - "The downloaded file has a .pdf extension that matches its content"
    - "The filename presented to the user is consistent with what is advertised"
  artifacts:
    - path: "public/peter-key-cv.pdf"
      provides: "Actual PDF file served by Next.js static file handler"
    - path: "src/app/components/Navbar.tsx"
      provides: "CV download link pointing to the real PDF with matching download attribute"
  key_links:
    - from: "src/app/components/Navbar.tsx downloadCV()"
      to: "public/peter-key-cv.pdf"
      via: "anchor href set to /peter-key-cv.pdf"
      pattern: "a\\.href.*\\.pdf"
---

<objective>
Repair the broken CV download so the primary recruiter CTA delivers an actual PDF file with a consistent filename.

Purpose: The CV download is the most important recruiter action on the site. It currently links to cv.txt, which either downloads a plain-text file or fails silently. This breaks the primary conversion path.
Output: A real PDF in public/ with the Navbar updated to link to it correctly.
</objective>

<execution_context>
@/home/peter/.claude/get-shit-done/workflows/execute-plan.md
@/home/peter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Place PDF in public/</name>
  <files>public/peter-key-cv.pdf</files>
  <action>
    The executor cannot create a real CV PDF — this requires the user to provide the file.

    Steps:
    1. Locate your current CV as a PDF file on your machine.
    2. Copy or rename it to: peter-key-cv.pdf
    3. Place it at: /home/peter/Documents/portfolio-website/public/peter-key-cv.pdf

    The filename must be exactly: peter-key-cv.pdf (lowercase, hyphenated)
  </action>
  <verify>
    <automated>ls /home/peter/Documents/portfolio-website/public/peter-key-cv.pdf</automated>
  </verify>
  <done>File public/peter-key-cv.pdf exists and is a valid PDF.</done>
  <resume-signal>Type "done" once the file is in public/ and you can see it with: ls /home/peter/Documents/portfolio-website/public/peter-key-cv.pdf</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update Navbar download link to real PDF</name>
  <files>src/app/components/Navbar.tsx</files>
  <action>
    In src/app/components/Navbar.tsx, locate the downloadCV function (around line 135):

    ```ts
    const downloadCV = () => {
      const a = document.createElement("a");
      a.href = "/cv.txt";
      a.download = "Peter_Williams-Key_CV.pdf";
      ...
    ```

    Make two changes:
    1. Change a.href from "/cv.txt" to "/peter-key-cv.pdf"
    2. Change a.download from "Peter_Williams-Key_CV.pdf" to "Peter-Key-CV.pdf"
       (matches the public/ filename pattern; consistent, no underscores)

    Do not change any other logic in the function. Do not touch any other part of Navbar.tsx.

    Note: Using "/peter-key-cv.pdf" per user's confirmed file placement decision.
    The download attribute value is what the browser presents as the save-as filename —
    it must end in .pdf and should be human-readable. "Peter-Key-CV.pdf" satisfies both.
  </action>
  <verify>
    <automated>grep -n "peter-key-cv.pdf" /home/peter/Documents/portfolio-website/src/app/components/Navbar.tsx</automated>
  </verify>
  <done>
    - a.href is "/peter-key-cv.pdf"
    - a.download is "Peter-Key-CV.pdf"
    - grep confirms both strings appear in Navbar.tsx
    - No other lines in the file reference cv.txt
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify download works end-to-end</name>
  <files>public/peter-key-cv.pdf</files>
  <action>Human verifies the download flow works correctly in the browser.</action>
  <verify>
    <automated>grep -c "cv.txt" /home/peter/Documents/portfolio-website/src/app/components/Navbar.tsx || echo "0 references to cv.txt (correct)"</automated>
  </verify>
  <done>User confirms PDF downloads and opens correctly, with filename Peter-Key-CV.pdf.</done>
  <what-built>PDF placed in public/ and Navbar link updated to point to it with a consistent filename.</what-built>
  <how-to-verify>
    1. Start the dev server if not running: npm run dev
    2. Open http://localhost:3000 in a browser
    3. Click "Download CV" in the navbar
    4. Confirm: a PDF download begins (browser shows a download prompt or the file appears in Downloads)
    5. Open the downloaded file — it should open in a PDF viewer, not a text editor
    6. Confirm the saved filename is "Peter-Key-CV.pdf" (not cv.txt or any .txt variant)
  </how-to-verify>
  <resume-signal>Type "approved" if the PDF downloads and opens correctly, or describe the issue if something is wrong.</resume-signal>
</task>

</tasks>

<verification>
- public/peter-key-cv.pdf exists and is a valid PDF (file size > 10KB is a reasonable proxy)
- Navbar.tsx contains no reference to cv.txt
- Navbar.tsx href points to /peter-key-cv.pdf
- Navbar.tsx download attribute is "Peter-Key-CV.pdf"
- Manual download test passes (Task 3 checkpoint approved)
</verification>

<success_criteria>
1. Clicking "Download CV" in the navbar downloads a real PDF that opens correctly in a PDF viewer — satisfies FIX-01
2. The downloaded filename is "Peter-Key-CV.pdf" — consistent .pdf extension with no mismatch — satisfies FIX-02
</success_criteria>

<output>
After completion, create .planning/phases/01-emergency-fix/01-01-SUMMARY.md
</output>
